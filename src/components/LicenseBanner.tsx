import { Badge } from '@/components/ui/badge';
import { Clock, Zap, Shield } from 'lucide-react';

export const LicenseBanner = () => {
  // License banner disabled - Direct access mode
  return null;
};
